% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/angle_calculation.R
\name{calculate_angle}
\alias{calculate_angle}
\title{Calculate angles from 2D data points}
\usage{
calculate_angle(data, alpha = 0.95, method = "mean", start = c(0, 0))
}
\arguments{
\item{data}{A matrix or data frame with 2 columns containing x and y coordinates}

\item{alpha}{Numeric value between 0 and 1 specifying the quantile threshold (default = 0.95)}

\item{method}{Character string specifying the angle calculation method. 
Options: "min", "median", "equal", "mean", "neigh" (default = "mean")}

\item{start}{Numeric vector of length 2 specifying the starting point coordinates (default = c(0,0))}
}
\value{
A matrix containing angle information for the selected method
}
\description{
This function calculates angles from 2D data points using various methods.
It filters data based on a starting point and computes angles using the specified method.
}
\examples{
\dontrun{
# Generate sample data
set.seed(123)
sample_data <- matrix(rnorm(200), ncol = 2)

# Calculate angles using default parameters (mean method)
result <- calculate_angle(sample_data)

# Calculate angles using min method
result_min <- calculate_angle(sample_data, alpha = 0.9, method = "min")
}

}
